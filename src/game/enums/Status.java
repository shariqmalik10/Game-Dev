package game.enums;

/**
 * Use this enum class to give `buff` or `debuff`.
 * It is also useful to give a `state` to abilities or actions that can be attached-detached.
 */
public enum Status {
    HOSTILE_TO_ENEMY,
    HOSTILE_TO_UNKINDLED,
    PRONE_TO_FALL,
    UNKINDLED,
    PRONE_TO_BURN,
    STUNNED,
    WEAK_TO_STORMRULER,
    LORD_OF_CINDER,
    ENEMIES,
}

